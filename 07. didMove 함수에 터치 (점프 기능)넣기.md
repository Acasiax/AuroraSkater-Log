# 📍didMove 함수에 터치 (점프 기능)넣기
```swift
let tapGestureRecognizer = UITapGestureRecognizer(target: self, action: #selector(jump(_:)))
        view.addGestureRecognizer(tapGestureRecognizer)
```
